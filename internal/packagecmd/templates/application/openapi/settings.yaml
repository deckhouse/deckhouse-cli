$schema: http://json-schema.org/schema#
type: object
properties:
  replicas:
    type: integer
    default: 1
    description: Number of server replicas to run.
  msg:
    type: string
    description: Text the server answers every request with.
