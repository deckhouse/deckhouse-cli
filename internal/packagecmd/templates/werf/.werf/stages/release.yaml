---
image: release
fromImage: scratch
import:
  - image: definition
    add: /
    to: /
    after: install
    includePaths:
      - version.json
      - changelog.yaml
      - package.yaml
      - openapi
      - docs
imageSpec:
  config:
    labels:
      io.deckhouse.package.type: '{{ (.Files.Get "package.yaml" | fromYaml).type }}'
      io.deckhouse.package.apiVersion: v1
